services:
  app:
    build:
      context: .
    image: project-management-mvp:local
    container_name: project-management-mvp
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
